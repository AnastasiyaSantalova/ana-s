export type PortfolioItem = {
  name: string;
  description: string;
  src: string;
}

export type TagsCloudItem = {
  name: string;
  weight: number;
}

export type ButtonTypes = 'close' | 'cta' | 'menu';
